#==========================================================================================#
#==========================================================================================#
#     This function diverts the standard output to nowhere.                                #
#------------------------------------------------------------------------------------------#
shhh <<- function(fun,...){
    nowhere   = tempfile()
    dummy     = sink(file=nowhere)
    
    dotdotdot = list(...)
    ans       = do.call(what=fun,args=dotdotdot)
    
    dummy     = sink(file=NULL)
    dummy     = file.remove(nowhere)
    return(ans)
}#end shhh
#==========================================================================================#
#==========================================================================================#
